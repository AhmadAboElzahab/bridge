export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "Bridge",
  description: "Bridge — workforce management platform",
  url: "http://localhost:3000",
  links: { github: "" },
};
